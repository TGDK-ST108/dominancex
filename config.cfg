# =====================================================
# TGDK :: DominanceX Configuration File
# AGENT: H6
# LICENSE: BFE-TGDK-022ST
# Updated for: Mobile sync, encryption, domain defense
# =====================================================

# Whitelisted IPs allowed through firewall
WHITELIST_IP=127.0.0.1,192.168.1.1

# Whitelisted ports allowed for outbound/inbound
WHITELIST_PORT=22,53

# Domains to block or deny resolution/traffic
BLOCK_DOMAINS=example.com,badsite.org,amazon.com,aws.com

# Log output file (adjusted for Termux/mobile safe path)
LOG_PATH=/data/data/com.termux/files/home/tgdk105/blocked.log

# Optional encryption key for logs
ENCRYPT_KEY=QQUAp

# Remote sync credentials and target path
SYNC_REMOTE_USER=your_username
SYNC_REMOTE_HOST=your.remote.server
SYNC_REMOTE_PATH=/home/your_username/dominancex_logs/